# FoodFlow retains a few intentional presentation conventions in long-form docs:
# long command lines, HTML-backed badges, and compact tables. Keep structural,
# heading, link, list, fence, and whitespace rules enabled everywhere else.
config:
  MD013: false
  MD024:
    siblings_only: true
  MD033: false
  MD060: false
